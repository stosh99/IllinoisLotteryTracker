# Optional. Leave empty for the same-origin /api/v1/rankings endpoint.
# Set a full URL only when the API is hosted on a different origin.
VITE_RANKINGS_URL=

# Development-only proxy target. The default is http://127.0.0.1:8000.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
